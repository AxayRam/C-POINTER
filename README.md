Instructions
Create a code space (reuse if already created):
code -> codespaces -> + button!
Add code in main.c in the main() function.
Use gcc main.c to compile the code.
Execute ./a.out to run the program
